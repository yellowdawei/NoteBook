# 🎯 Git 使用完整总结

## 📚 核心概念

### Git 三大区域

- **工作区**：直接编辑文件的地方
- **暂存区**：准备提交的改动（`git add`）
- **仓库**：永久的版本历史（`git commit`）

### 基本工作流

text

```
编辑文件 → git add → git commit → git push
```



## 🔧 完整操作流程

### 1. 初始设置

bash

```
# 配置用户信息（一次即可）
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"

# 初始化仓库
git init
```



### 2. SSH 密钥配置

bash

```
# 生成密钥
ssh-keygen -t ed25519 -C "你的邮箱"
# 一路按 Enter 使用默认设置

# 启动代理
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 复制公钥
cat ~/.ssh/id_ed25519.pub
# 将输出内容添加到 GitHub SSH keys
```



### 3. 连接远程仓库

bash

```
# 添加远程仓库
git remote add origin git@github.com:用户名/仓库名.git

# 如果远程有内容（如 README），先拉取
git pull origin main --allow-unrelated-histories
```



### 4. 添加和提交文件

bash

```
# 添加所有文件
git add .

# 提交到本地仓库
git commit -m "有意义的提交信息"
```



### 5. 推送到远程

bash

```
# 重命名分支为 main（如果需要）
git branch -M main

# 首次推送
git push -u origin main

# 后续推送
git push
```



## 📝 日常使用命令

### 基本操作

bash

```
# 查看状态
git status

# 查看提交历史
git log

# 查看文件改动
git diff
```



### 分支管理

bash

```
# 创建并切换分支
git checkout -b 新分支名

# 切换分支
git checkout 分支名
# 或
git switch 分支名

# 查看所有分支
git branch

# 合并分支
git merge 分支名
```



### 远程操作

bash

```bash
# 查看远程仓库
git remote -v

# 拉取更新
git pull

# 推送更改
git push
```